const max = 100;
var arg, ret, i;
procedure ppp;
    const max2 = 13;
    var arg2, ret2, i2;
    procedure ppp2;
    begin
        if max2 < max then
            write(max2)
        else
            begin
                write(max);
                write(max2)
            end
    end;
begin
    write(arg2, ret2, i2);
    read(arg2, i2, ret2)
end;
begin
    while arg < ret do
        i := i * i * i + i;
    if arg < ret then
        begin
            write(arg, ret);
            read(arg, ret)
        end
end
.